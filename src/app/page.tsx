import type { FC } from 'react';

import Typography from '@mui/material/Typography';

const Home: FC = () => (
    <div>
        <main>
            <Typography variant="h1">Welcome to Talkarr</Typography>
        </main>
    </div>
);

export default Home;
