name: PR Conventional Commit Validation

on:
  pull_request:
    types: [opened, synchronize, reopened, edited]

permissions: {}

jobs:
  validate-pr-title:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - name: Generate a token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: PR Conventional Commit Validation
        uses: ytanikin/PRConventionalCommits@1.3.0
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
        with:
          task_types: '["feat","fix","docs","ci","refactor","perf","chore","revert"]'
          add_label: 'false'

      - name: Extract the type from the PR title and set as label
        uses: actions/github-script@v8
        env:
          TITLE: ${{ github.event.pull_request.title }}
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
        with:
          script: |
            const title = process.env.TITLE;
            let typeMatch = title.match(/^(\w+)(\(.+\))?:/);
            
            const supportedLabels = [
              "feat",
              "fix",
              "docs",
              "ci",
              "refactor",
              "perf",
              "chore",
              "revert"
            ];
            
            const labelMap = {
              "feat": "feature",
              "fix": "bugfix",
            };
            
            const labelType = typeMatch ? typeMatch[1] in labelMap ? labelMap[typeMatch[1]] : typeMatch[1] : null;

            // remove existing supported labels and add new ones if not present based on PR title
            const existingLabels = await github.rest.issues.listLabelsOnIssue({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            if (labelType) {
              if (supportedLabels.includes(labelType)) {
                await github.rest.issues.addLabels({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: context.issue.number,
                  labels: [labelType],
                });
              }
            }
            
            const labelsToRemove = existingLabels.data
              .map(label => label.name)
              .filter(name => supportedLabels.includes(name) && (!labelType || name !== labelType));

            for (const label of labelsToRemove) {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                name: label,
              });
            }

            if (labelsToRemove.length > 0) {
              console.log(`Removed labels: ${labelsToRemove.join(", ")}`);
            }
            if (typeMatch) {
              console.log(`Added label: ${labelType}`);
            } else {
              console.log("No valid type found in PR title to add as label.");
            }
